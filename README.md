#include<stdio.h>
int main()
{
	int a = 80;
	float f = 45.3;
	float suma = (float)a + f;
	printf("%.2f", suma);
	//printf("entero %i\n floatante &f2\n, double %3f\n, caracter %c\n 
	//",a, (float)a, (double)a,)
	return 0;
}
